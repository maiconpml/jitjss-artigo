\subsection{Automated Configuration Results}
\label{subsec:irace_results}

The automated algorithm configuration process conducted via \texttt{irace} yielded five elite metaheuristic configurations that demonstrated superior performance on the training instance set.
Table~\ref{tab:irace_elites_vertical} details the complete parameter settings for these five elite configurations, omitting uninstantiated parameters.

\begin{table}[ht]
\centering
\caption{Parameter configurations of the top five elite algorithms generated by \texttt{irace}.}
\label{tab:irace_elites_vertical}
\resizebox{\linewidth}{!}{
\begin{tabular}{lccccc}
\hline
\textbf{Parameter} & \textbf{$C_1$} & \textbf{$C_2$} & \textbf{$C_3$} & \textbf{$C_4$} & \textbf{$C_5$} \\ \hline
\texttt{initial\_sol} & \texttt{gt} & \texttt{gt} & \texttt{gt} & \texttt{gt} & \texttt{gt} \\ 
\texttt{dispatch\_rule} & \texttt{edd} & \texttt{edd} & \texttt{edd} & \texttt{edd} & \texttt{edd} \\ 
\texttt{search$_1$} & \texttt{ils} & \texttt{ils} & \texttt{ils} & \texttt{ils} & \texttt{ils} \\ 
\texttt{search$_2$} & \texttt{tabu} & \texttt{tabu} & \texttt{tabu} & \texttt{tabu} & \texttt{tabu} \\ 
\texttt{max\_milli\_ils\_iteration} & \texttt{19900} & \texttt{21254} & \texttt{9713} & \texttt{5741} & \texttt{14594} \\ 
\texttt{neighborhood\_number} & \texttt{2} & \texttt{2} & \texttt{3} & \texttt{3} & \texttt{3} \\ 
\texttt{neighborhood$_1$} & \texttt{swap\_penal} & \texttt{swap\_penal} & \texttt{swap\_penal} & \texttt{swap\_rand} & \texttt{swap\_penal} \\ 
\texttt{neighborhood\_exploration$_1$} & \texttt{fi} & \texttt{fi} & \texttt{fi} & \texttt{bi} & \texttt{fi} \\ 
\texttt{neighborhood$_2$} & \texttt{swap\_crit} & \texttt{swap\_crit} & \texttt{swap\_crit} & \texttt{swap\_adj} & \texttt{swap\_crit} \\ 
\texttt{neighborhood\_exploration$_2$} & \texttt{bi} & \texttt{fi} & \texttt{fi} & \texttt{fi} & \texttt{fi} \\ 
\texttt{neighborhood$_3$} & \texttt{-} & \texttt{-} & \texttt{swap\_rand} & \texttt{swap\_penal} & \texttt{swap\_rand} \\ 
\texttt{neighborhood\_exploration$_3$} & \texttt{-} & \texttt{-} & \texttt{fi} & \texttt{bi} & \texttt{fi} \\ 
\texttt{scheduler} & \texttt{linear} & \texttt{linear} & \texttt{linear} & \texttt{linear} & \texttt{linear} \\ 
\texttt{max\_d} & \texttt{29} & \texttt{19} & \texttt{44} & \texttt{65} & \texttt{38} \\ 
\texttt{max\_c} & \texttt{10} & \texttt{2} & \texttt{3} & \texttt{3} & \texttt{2} \\ 
\texttt{tenure} & \texttt{98} & \texttt{96} & \texttt{98} & \texttt{70} & \texttt{85} \\ 
\texttt{elite\_list\_size} & \texttt{23} & \texttt{22} & \texttt{21} & \texttt{13} & \texttt{21} \\ 
\texttt{limit\_iters} & \texttt{2788} & \texttt{2164} & \texttt{1935} & \texttt{2749} & \texttt{1353} \\ 
\texttt{decrease\_divisor} & \texttt{16} & \texttt{12} & \texttt{8} & \texttt{10} & \texttt{11} \\ \hline
\end{tabular}
}
\end{table}

Cross-configuration analysis reveals remarkable structural uniformity across high-level categorical components.
All five elite configurations selected identical options for initial solution construction, top-level search container, inner search procedure, and scheduler algorithm.
This universal agreement confirms that active schedule generation via Giffler--Thompson filtering, fast linear-time sequence scheduling, and Tabu Search embedded within an Iterated Local Search container constitute essential building blocks for JITJSS optimization.

Structural analysis of neighborhood choices highlights clear operator preferences.
The penalty-driven operator \texttt{swap\_penal} appears in 100\% of elite configurations, while our novel critical-path operator \texttt{swap\_crit} is selected in 80\% of the elite set.
This strong preference confirms that prioritizing penalized operations and swapping bottleneck operations along individual critical paths are decisive for minimizing earliness and tardiness penalties in JITJSS.
Conversely, \texttt{irace} discarded insertion operators (\texttt{insert\_rand}, \texttt{insert\_penal}) and the computationally slow \texttt{relax} neighborhood, favoring faster, targeted swap mechanics.
In terms of overall architecture, the elite configurations represent a hybrid Iterated Tabu Search (ITS) with a multi-neighborhood cyclic VNS progression, combining multiple neighborhood operators with different exploration modes.

To quantify structural convergence across configuration iterations rigorously, we evaluated pairwise configuration dissimilarity using Gower's General Distance Coefficient \citep{gower1971general} alongside the Jaccard Distance Index \citep{jaccard1901etude}.
Gower's distance measures overall dissimilarity across mixed categorical and numerical parameter spaces according to Equation~\eqref{eq:gower}:

\begin{equation} \label{eq:gower}
    d(i,j) = \frac{\sum_{k=1}^n \delta_{ij}^{(k)} s_{ij}^{(k)}}{\sum_{k=1}^n \delta_{ij}^{(k)}}
\end{equation}

where $n$ represents the total number of design space parameters, $\delta_{ij}^{(k)} \in \{0, 1\}$ indicates parameter applicability in both configurations $i$ and $j$, and $s_{ij}^{(k)}$ denotes parameter-specific dissimilarity.
For categorical parameters, $s_{ij}^{(k)} = 0$ if values match ($x_{ik} = x_{jk}$) and $1$ otherwise.
For numerical parameters, $s_{ij}^{(k)} = \frac{|x_{ik} - x_{jk}|}{R_k}$, normalized by the parameter search range $R_k$.

\begin{figure}[htb] 
    \centering 
    \includegraphics[width=0.85\linewidth]{img/config_convergence.pdf}
    \caption{Evolution of average pairwise Gower and Jaccard distances among elite configurations across \texttt{irace} iterations.}
    \label{fig:grafico_variancia}
\end{figure}

Because Gower's distance evaluates parameter positions independently, it can overestimate dissimilarity when two configurations select identical neighborhood sets in different permutation orders.
Due to the cyclic progression of our VNS container, different permutations of the same neighborhood set exhibit functionally equivalent search behavior.
To complement Gower's metric, we compute the Jaccard Distance Index \citep{jaccard1901etude} over active neighborhood sets $U$ and $V$, evaluating set-level dissimilarity independently of derivation order according to Equation~\eqref{eq:jaccard}:

\begin{equation} \label{eq:jaccard}
    Jaccard(U,V) = 1 - \frac{|U \cap V|}{|U \cup V|}
\end{equation}


Figure~\ref{fig:grafico_variancia} tracks the average pairwise Gower and Jaccard distances across all 11 \texttt{irace} configuration iterations.
Both distance metrics exhibit a sharp, monotonic reduction during initial iterations followed by clear stabilization throughout the second half of the configuration process.
This stabilization confirms that the automated configurator converged toward a mature, high-performing consensus region within the JITJSS algorithmic design space.

\subsection{Literature benchmark results}
\label{subsec:benchmark_tests}

To evaluate the generalization capacity and solution quality of the derived algorithms, we executed all five elite configurations on the standard 72 JITJSS benchmark test instances of \citet{baptiste2008lagrangian}.
In our performance analysis, each configuration is evaluated against the best-known solution value for each instance, defined as the minimum objective value obtained across the combination of all five state-of-the-art literature methods: \citet{dos2010combination}, \citet{wang2014variable}, \citet{ahmadian2021meta}, \citet{abderrazzak2022adaptive}, and \citet{sabri2023reinforcement}.
Because the algorithms incorporate stochastic search components, each configuration was executed 5 times per instance using 5 distinct random seeds.

\input{table_results_confs.tex}

Table~\ref{tab:results_confs} summarizes the comparative performance of the five elite configurations across the test set.
For each configuration, the table reports: (i) the number of instances where the best solution ($B$) achieved across 5 runs matched or improved upon the best-known literature benchmark; and (ii) the number of instances where the median solution ($M$) across 5 runs matched or improved upon the literature benchmark.

As shown in Table~\ref{tab:results_confs}, configuration $C_1$ demonstrated superior generalization performance on unseen test instances, attaining the highest number of top-performing solutions.
Because $C_1$ achieved the best overall performance among all elite configurations, we selected $C_1$ as our primary algorithm representative for detailed instance-by-instance comparison against the state of the art.

\input{table_results_10.tex}

\input{table_results_15.tex}

\input{table_results_20.tex}

Tables~\ref{tab:results_10}, \ref{tab:results_15}, and \ref{tab:results_20} report detailed benchmark comparison results for instances with 10, 15, and 20 jobs, respectively.
In these tables, literature benchmarks are represented respectively by: CPLEX exact lower/upper bounds, \citet{dos2010combination}, \citet{wang2014variable}, \citet{ahmadian2021meta}, \citet{abderrazzak2022adaptive}, and \citet{sabri2023reinforcement}.
The last two columns report our algorithm's Best ($B$) and Median ($M$) objective values across 5 independent runs, with best-known values highlighted in bold.


The numerical results presented in Tables~\ref{tab:results_10}--\ref{tab:results_20} demonstrate the strong performance of our automatically configured algorithm.
Overall, configuration $C_1$ matched or improved upon the best-known literature bounds in 72.2\% of the 72 test instances.
This success rate is particularly high on small-scale instances, where $C_1$ matched or exceeded the literature best in 83.3\% of cases.
Analysis of median results further confirms algorithmic robustness: across 5 stochastic runs, the median solution matched or improved upon the literature best in 44.4\% of instances, with an average percentage deviation of only 1.06\% relative to the best individual runs.

In addition to comparing against composite best-known bounds, Table~\ref{tab:comparacao_estado} reports a direct head-to-head pairwise comparison between configuration $C_1$ and each literature algorithm independently.

\input{table_comparison_state.tex}

The head-to-head results in Table~\ref{tab:comparacao_estado} confirm that our automatically designed algorithm outperforms or matches every individual literature method across a decisive majority of benchmark instances.
Specifically, $C_1$ achieves superior or equal solution quality in 94.4\% of instances compared to \citet{dos2010combination}, 97.2\% compared to \citet{wang2014variable}, 77.7\% compared to \citet{ahmadian2021meta}, 79.1\% compared to \citet{abderrazzak2022adaptive}, and 81.2\% compared to \cite{sabri2023reinforcement}. Furthermore, $C_1$ executes in an average computation time of only 21.1 seconds, offering a reduced wall-clock time relative to exact solvers and competing metaheuristics.
These empirical findings validate our initial hypothesis: bottom-up Automatic Algorithm Design via context-free BNF grammars and \texttt{irace} configuration effectively synthesizes novel metaheuristics that surpass human-designed algorithms in both solution quality and efficiency.
